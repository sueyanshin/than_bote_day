import 'package:flutter/material.dart';

class FontSizeChanger extends StatelessWidget {
  const FontSizeChanger({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(width: 100, height: 100, child: Text('helel'));
  }
}
